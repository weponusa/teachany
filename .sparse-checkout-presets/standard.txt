# TeachAny · Sparse Checkout Preset · standard ⭐ 推荐
# 制作器内核 + 全部地图 + 知识树 + 课标摘录，约 110MB（工作目录）
# 适用场景：所有学科课件制作（含地理/历史/政治/自然地理）
#
# ⚠️ 本文件仅使用 cone 模式兼容语法（纯目录路径）
#   · 不使用 ! 否定（cone 模式不支持）
#   · 不使用 * 通配符（cone 模式不支持）
#   · 未列出的目录自动排除（community/ examples/ 等无需显式否定）
#
# 用法（cone 模式）：
#   git sparse-checkout init --cone
#   git sparse-checkout set skill/ scripts/ data/ assets/maps/ references/ docs/ .sparse-checkout-presets/

# ── 制作器核心 ──
skill/
scripts/
references/
docs/

# ── 知识树 + 课标摘录 + 知识点索引 ──
data/

# ── 全部地图资产（历史 + 现代政区 + 自然地理底图） ──
assets/maps/

# ── sparse checkout 预设文件本身 ──
.sparse-checkout-presets/
